#this permission is for database syncing with config this happens on restart or on the sync command
#If false the Database will try rewrite the config file.
#if true all changes in the config will be written to the database
syncConfigToDb: true;
